# quiz-leaderboard-system
Got it — here’s a **clean description under 350 characters**:  **Built a Java-based system to poll an external API, handle duplicate responses using deduplication logic, and generate a leaderboard by aggregating participant scores. Implemented retry handling for API failures and ensured consistent results across multiple polls.
